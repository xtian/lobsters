{
  "name": "Lobsters",
  "repository": "https://github.com/xtian/lobsters",
  "scripts": {
    "dokku": {
      "postdeploy": "bin/rake db:migrate"
    }
  },
  "env": {
    "RACK_ENV": "production",
    "RAILS_ENV": "production",
    "RAILS_MAX_THREADS": 5,
    "RAILS_SERVE_STATIC_FILES": "true"
  }
}
